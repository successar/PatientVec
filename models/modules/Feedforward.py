from allennlp.modules import FeedForward
# import torch.nn as nn
# from allennlp.common.from_params import FromParams

# class Decoder(nn.Module, FromParams) :
#     def __init__(self, input_dim, num_layers, hidden_dims, activations, dropout = 0.0, structured=False) :
